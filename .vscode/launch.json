{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Watch (Handlevett)",
      "type": "coreclr",
      "request": "attach",
      "processId": "${command:pickProcess}",
      "preLaunchTask": "watch"
    },
    {
      "name": "Run (kill & restart)",
      "type": "coreclr",
      "request": "attach",
      "processId": "${command:pickProcess}",
      "preLaunchTask": "kill-and-run"
    }
  ]
}
